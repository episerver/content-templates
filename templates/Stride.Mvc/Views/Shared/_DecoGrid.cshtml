<div class="fixed inset-0 pointer-events-none -z-10">
    <div class="container h-full flex justify-between gap-6">
        @for (var i = 0; i < 6; i++)
        {
            <div class="@(i < 4 ? "hidden md:block" : "") grow border border-foreground/15 border-y-0 border-dashed"></div>
        }
    </div>
</div>
